require("dotenv").config();

exports.encryptThis = async function(input){
    console.log("--------------ENCRYPT THIS THING----------------");
    if (await bcrypt.compare(password, hashedPassword)) {
        
    }

    // .then(url => {
    //   console.log(url)
    // })
    // .catch(err => {
    //   console.error(err)
    // })
    return;

};